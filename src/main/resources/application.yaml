server:
  # ??
  port: 8080

spring:
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher

  # ?????
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/brand?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
    username: root
    password: root
    hikari:
      # ????
      pool-name: DateHikariCP
      # ???????
      minimum-idle: 5
      # ?????????????600000?10???
      idle-timeout: 180000
      # ????????10
      maximum-pool-size: 10
      # ??????????????
      auto-commit: true
      # ?????????0?????????1800000?30???
      max-lifetime: 1800000
      # ?????????30000?30??
      connection-timeout: 30000
      # ?????????????
      connection-test-query: SELECT 1


# Mybatis-plus??
mybatis-plus:
  #??Mapper????
  mapper-locations: classpath*:/mapper/*Mapper.xml
  # ??MyBatis?????????????????
  type-aliases-package: com.example.server.pojo
  configuration:
    # ??????
    map-underscore-to-camel-case: false

## Mybatis SQL ??(???????????Mapper.xml????)
logging:
  level:
    com.example.server.mapper: debug





